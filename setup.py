import setuptools

setuptools.setup(
    name="research_utils",
    version="0.0.1",
    description="A collection of common utils, used during my research",
    packages=setuptools.find_packages(),
    python_requires='>=3.6'
)
